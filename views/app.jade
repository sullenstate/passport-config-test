extends layout

block content
	.centered
		if user.displayName
			h1 #{user.displayName} Be Logged In!
		else
			h1 #{user.username} Be Logged In!

		a.btn.btn-default.btn-lg.btn-theme(href="logout" type="button") Logout!
	